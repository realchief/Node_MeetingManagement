var FT = FT || {};
FT.data = FT.data || {}

/**
 * Set up the buckets and platform
 * platform are tied to a bucket and contain generic metric metrics
 * buckets are combinations of platform which define business sector KPIs
 */


FT.data.data_sources = {}

