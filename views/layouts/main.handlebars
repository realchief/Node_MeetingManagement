{{> head }}

<body class="home">

	<div class="app-container">

		<div class="menu">
			{{> menu }}
		</div>

		<div class="main">

			{{#if extraClass}}
			<div class="{{extraClass}}">
			{{/if}}

			<div class="content">
				{{{ body }}}
			</div>
		
			{{#if extraClass}}
			</div>
			{{/if}}

		</div>
		</div>

	</div>	

</div>

{{> scripts }}

</body>
</html>
